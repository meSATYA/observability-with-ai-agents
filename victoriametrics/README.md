# VictoriaMetrics

This lab uses VictoriaMetrics Single as the local remote-write store. Its durable
data path is the named Docker volume `victoriametrics-data`; no custom server
configuration is required for this single-node development deployment.
