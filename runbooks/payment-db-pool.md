# Payment database connection-pool exhaustion

Evidence required: payment error spans/logs plus elevated checkout latency or error rate. Confirm active connections and leak behavior before modifying pool size. Mitigation requires human approval.
